// App configuration constants
export const APP_CONFIG = {
  GOAL_COUNT: 5,
  APP_NAME: 'Alt+Shift',
  VERSION: '1.0.0',
} as const;

// Routes
export const ROUTES = {
  HOME: '/',
  CREATE: '/create',
} as const;

// Local storage keys
export const STORAGE_KEYS = {
  THEME: 'alt-shift-theme',
  USER_PREFERENCES: 'alt-shift-preferences',
} as const;
