export type { Letter } from '../../shared/lib/types';
export type { LetterFormData } from '../../shared/lib/schemas';
export { letterService } from '../../shared/api/letterService';
export { generateLetter } from '../../shared/lib/letterGenerator';
